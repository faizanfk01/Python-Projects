# Tuples are ordered, immutable collections that can store multiple data types.
# Once created, their values cannot be changed (no append, remove, etc.).

my_tuple = (10, 20, 30, 20, 40)
print(my_tuple)